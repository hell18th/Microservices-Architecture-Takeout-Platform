<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.shoppingCar.mapper.ShoppingCarMapper">
    <select id="list">
        select *
        from shopping_car
        <where>
            <if test="setMealId != null">
                and set_meal_id = #{setMealId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
        </where>
    </select>

    <insert id="insertBatch">
        insert into shopping_car(name, image, user_id, dish_id, set_meal_id, dish_flavor, number, amount, create_time)
        values
        <foreach collection="shoppingCarlist" separator="," item="s">
            (#{s.name}, #{s.image}, #{s.userId}, #{s.dishId}, #{s.setMealId}, #{s.dishFlavor}, #{s.number}, #{s.amount},
            #{s.createTime})
        </foreach>
    </insert>
</mapper>