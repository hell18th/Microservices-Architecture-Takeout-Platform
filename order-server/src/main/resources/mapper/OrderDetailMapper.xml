<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.order.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail(name, image, order_id, dish_id, set_meal_id, dish_flavor, number, amount)
        values
        <foreach collection="orderDetails" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.dishId}, #{od.setMealId}, #{od.dishFlavor}, #{od.number},
            #{od.amount})
        </foreach>
    </insert>

    <select id="getSales">
        select od.name, sum(od.number) number
        from order_detail od,
        orders o
        where od.order_id = o.id
        and o.status = #{status}
        <if test="dayBegin != null">and o.order_time &gt; #{dayBegin}</if>
        <if test="dayEnd != null">and o.order_time &lt; #{dayEnd}</if>
        group by od.name
        order by number desc
        limit 0, 10
    </select>
</mapper>